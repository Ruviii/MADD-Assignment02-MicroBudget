
import SwiftUI

extension Color {
    // Background Colors
    static let appBackground = Color(red: 0.06, green: 0.08, blue: 0.10) // #0F1419

    // Accent Colors
    static let primaryAccent = Color(red: 0.4, green: 0.4, blue: 1.0) // Purple-Blue
    static let accentGradientStart = Color(red: 0.5, green: 0.4, blue: 1.0)
    static let accentGradientEnd = Color(red: 0.3, green: 0.5, blue: 1.0)

    // Text Colors
    static let primaryText = Color.white
    static let secondaryText = Color(red: 0.6, green: 0.6, blue: 0.65)
    static let tertiaryText = Color(red: 0.45, green: 0.45, blue: 0.5)
}
